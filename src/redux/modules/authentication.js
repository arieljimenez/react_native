const initialState = {
  isAuthed: false,
  isAuthenticating: false,
  authedID: '',
}

export default function authentication(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}